build/*
results/*
old_results/*
hypergraphs/*

# Python cache
__pycache__/
*.pyc

# Computed plot data + LaTeX build products under the reproducibility tree.
# These are regenerated by make_all_paper.py from the raw results/ tsv files,
# so we keep them out of git (only the raw inputs and the .tex are tracked).
latex/data/plot_data/
latex/*.aux
latex/*.log
latex/*.out
latex/*.fls
latex/*.fdb_latexmk
latex/*.pdf
latex/*.synctex.gz